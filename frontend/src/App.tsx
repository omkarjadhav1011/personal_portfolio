import ScratchProjects from "@/pages/ScratchProjects";

/**
 * Phase 7.2 — renders the scratch page that exercises the API client +
 * TanStack Query against GET /api/projects. Real routing lands in a later step.
 */
export default function App() {
  return <ScratchProjects />;
}
