/** Placeholder for admin sections not yet ported (experience/skills/profile). */
export default function ComingSoon() {
  return (
    <div className="font-mono text-sm space-y-2">
      <p className="text-text-faint text-xs">$ git checkout --not-yet</p>
      <p className="text-text-muted">This admin section is ported in a later phase.</p>
    </div>
  );
}
